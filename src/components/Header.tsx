import styled from "styled-components";

const Container = styled.header`
`

export const Header = () => {
    return (
        <Container>
            Header
        </Container>
    )
}
